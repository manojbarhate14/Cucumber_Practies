package pageObject;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

// Page Object model without page factory
public class LoginPageWithoutPF {
	
	/*WebDriver ldriver;
	
	public LoginPageWithoutPF(WebDriver driver)
	{
		ldriver= driver;
	}
	
	By email = By.id("Email");
	By password = By.id("Password");
	By LoginBtn = By.xpath("//button[@class='button-1 login-button']");
	By logout = By.linkText("Logout");
	

	public void enterEmail(String emailAdd) {
		ldriver.findElement(email).sendKeys(emailAdd);
	}

	public void enterPassword(String pwd) {
		ldriver.findElement(password).sendKeys(pwd);
	}

	public void clickOnLoginButton() {
		ldriver.findElement(LoginBtn).click();
	}

	public void clickOnLogOutButton() {
		ldriver.findElement(logout).click();
	}*/

}
